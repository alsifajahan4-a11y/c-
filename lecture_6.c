#include<stdio.h>
int main()
{ 
    int n;
    int prime no;
    printf("Enter the prime no:");
    scanf("%d",prime no);
    if(n<=1);
    {
        printf("it is not a prime no");
        return 0;
    }
    for(int i=2; i<=n; i++)
    {
        prime 
    }
}
